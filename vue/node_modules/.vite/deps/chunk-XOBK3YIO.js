import {
  k
} from "./chunk-K3RI3S3H.js";

// node_modules/vue-data-ui/dist/pdf-BcZrdj8V.js
async function j({ domElement: t, fileName: l, scale: i = 2, options: F = {} }) {
  if (!t) return Promise.reject("No domElement provided");
  const m = typeof navigator < "u" && /^((?!chrome|android).)*safari/i.test(navigator.userAgent);
  let g;
  try {
    g = (await import("./__vite-optional-peer-dep_jspdf_vue-data-ui_false-YLGTFFUF.js")).default;
  } catch {
    return Promise.reject("jspdf is not installed. Run npm install jspdf");
  }
  const a = {
    width: 595.28,
    height: 841.89
  };
  if (m)
    try {
      await k({ container: t, scale: i }), await k({ container: t, scale: i }), await k({ container: t, scale: i }), await k({ container: t, scale: i });
    } catch {
    }
  const s = await k({ container: t, scale: i });
  return await new Promise((c, P) => {
    const n = new window.Image();
    n.onload = function() {
      const o = n.naturalWidth, h = n.naturalHeight;
      let f = a.width, w = a.width / o * h;
      const r = new g("", "pt", "a4");
      let p = 0, d = h;
      const u = o / a.width * a.height;
      if (d < u)
        r.addImage(
          s,
          "PNG",
          0,
          0,
          f,
          w,
          "",
          "FAST"
        );
      else
        for (; d > 0; )
          r.addImage(
            s,
            "PNG",
            0,
            p,
            f,
            w,
            "",
            "FAST"
          ), d -= u, p -= a.height, d > 0 && r.addPage();
      r.save(`${l}.pdf`), c();
    }, n.onerror = (o) => P("Failed to load image for PDF: " + o), n.src = s;
  });
}

export {
  j
};
//# sourceMappingURL=chunk-XOBK3YIO.js.map
